<h1><%= t('.badge') %>: <%= @badge.title %></h1>
<%= link_to t('.create_badge'), new_admin_badge_path %>

<table class="table table-striped">
  <tr class="table-primary">
    <th><%= t('.title') %></th>
    <th><%= t('.image') %></th>
    <th><%= t('.rule') %></th>
    <th><%= t('.users') %></th>
    <th colspan="3"><%= t('.actions') %></th>
  </tr>
  <%= render @badge %>
</table>

<br>

<%= link_to t('.back'), admin_badges_path %>

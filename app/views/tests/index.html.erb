<h1><%= t('.header') %></h1>

<table class="table table-striped">
  <tr class="table-primary">
    <th><%= t('.title') %></th>
    <th><%= t('.category') %></th>
    <th><%= t('.author') %></th>
    <th><%= t('.level') %></th>
    <th><%= t('.count_questions') %></th>
    <th><%= t('.testing') %></th>
  </tr>
  <%= render @tests %>
</table>

<%= link_to t('.back'), :root %>

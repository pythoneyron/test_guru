<%= form_with model: @question, url: question_path, local: true do |form| %>
  <%= render 'shared/errors', resource: @question%>

  <p>
    <%= form.label :body %>
    <%= form.text_field :body, id: :question_body_new %>
  </p>

  <p><%= form.submit %></p>
<% end %>
